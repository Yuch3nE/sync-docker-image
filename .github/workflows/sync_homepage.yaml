name: 'Sync Image: [homepage:latest]'
run-name: Sync gethomepage/homepage:latest to yuch3n/homepage:latest
on: 
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 5" 

env:
  source: 'ghcr.io'
  destination: 'registry.cn-hangzhou.aliyuncs.com'
  source_repo: 'gethomepage/homepage:latest'
  destination_repo: 'yuch3n/homepage:latest'

jobs:
  copy:
    runs-on: ubuntu-latest
    steps:
    - name: 'Sync Image: [${{ env.source_repo }}]'
      uses: ikrong/docker-sync-action@main
      with:
        source: ${{ env.source }}
        source-credential: ${{ secrets.SOURCE_CREDENTIAL }}
        destination: ${{ env.destination }}
        destination-credential: ${{ secrets.DESTINATION_CREDENTIAL }}
        copy: "${{ env.source_repo }} ${{ env.destination_repo }}"
